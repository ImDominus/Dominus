import matplotlib.pyplot as plt

x = np.array([1, 2, 3])
y = np.array([1, 4, 9])

build new plot
plt.plot(x, y)

change label name
plt.xlabel('something', fontsize=14, color='red')
plt.ylabel('something')

change range of axis
plt.axis([0, 6, 0, 20])

make title of plot
plt.title('Histogram of something')

make legend
plt.legend()

change tick size
plt.xticks(np.arange(min(x), max(x)+1, 1.0))

change size of one bar
plt.hist(data, bins=[0, 10, 20, 30, 40, 50, 100])

view hist
plt.hist()
